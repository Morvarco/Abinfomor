#Questo che segue è un piccolo script bash per copiare due file da una directory nota ad un'altra

# specificare i nomi dei due file
file1="nome_file1"
file2="nome_file2"

# specificare la directory di origine e di destinazione
sorgente="percorso directory di origine"
destinazione="percorso directory di destinazione/"

# effettuare la copia dei due file specificati
cp "${dir_origine}/${file1}" "${dir_destinazione}"
cp "${dir_origine}/${file2}" "${dir_destinazione}"





[#Esempio di applicazione del codice sopra

file1="monopolo.py"
file2="Multipoly.py"
sorgente="/home/marco/Scaricati"
destinazione="/home/marco/Astrofisica"
cp "$sorgente/$file1" "$destinazione"
cp "$sorgente/$file2" "$destinazione"
